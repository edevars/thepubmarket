'use client'

import { ComprasView } from '@/components/compras/ComprasView'

/** Mis compras — historial y rastreo de órdenes del comprador (client: Bearer). */
export default function ComprasPage() {
  return (
    <main>
      <ComprasView />
    </main>
  )
}
